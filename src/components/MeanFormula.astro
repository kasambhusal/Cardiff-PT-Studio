---
import Button from '../../components/Button';
---

<section class="bg-[var(--primary-white)] py-24 px-6 overflow-hidden">
  <div class="max-w-5xl mx-auto text-center">
    
    <h2 class="text-gray-400 text-xs font-black uppercase tracking-[0.4em] mb-4">
      The System
    </h2>
    
    <div class="mb-12">
      <h3 class="text-6xl md:text-8xl font-black text-[var(--primary-black)] leading-none uppercase italic tracking-tighter">
        The <span class="text-[var(--primary-blue)]">M.E.A.N.</span> Formula
      </h3>
    </div>

    <div class="flex flex-col md:flex-row justify-between items-center gap-8 mb-16 border-y border-gray-200 py-10">
      <div class="group text-center">
        <span class="block text-4xl font-black text-[var(--primary-black)] transition-colors">M</span>
        <p class="text-[10px] font-bold uppercase tracking-widest text-gray-500">Mindset</p>
      </div>
      <div class="hidden md:block w-px h-8 bg-gray-300"></div>
      <div class="group text-center">
        <span class="block text-4xl font-black text-[var(--primary-black)] transition-colors cursor">E</span>
        <p class="text-[10px] font-bold uppercase tracking-widest text-gray-500">Exercise</p>
      </div>
      <div class="hidden md:block w-px h-8 bg-gray-300"></div>
      <div class="group text-center">
        <span class="block text-4xl font-black text-[var(--primary-black)] transition-colors">A</span>
        <p class="text-[10px] font-bold uppercase tracking-widest text-gray-500">Accountability</p>
      </div>
      <div class="hidden md:block w-px h-8 bg-gray-300"></div>
      <div class="group text-center">
        <span class="block text-4xl font-black text-[var(--primary-black)] transition-colors">N</span>
        <p class="text-[10px] font-bold uppercase tracking-widest text-gray-500">Nutrition</p>
      </div>
    </div>

    <p class="text-xl md:text-2xl text-[var(--primary-black)] max-w-2xl mx-auto font-medium leading-tight mb-12 italic">
      "Structure and support for real life. Results without sacrificing your time or career."
    </p>

    <!-- <button class="bg-[var(--primary-black)] hover:bg-black text-white px-12 py-5 rounded-none font-black text-xl transition-all hover:scale-98 shadow-lg uppercase tracking-tighter cursor-pointer">
      Join Cardiff Today
    </button> -->
    <Button client:visible variant='short'/>
  </div>
</section>